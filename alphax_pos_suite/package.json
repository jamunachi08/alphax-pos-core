{
  "name": "alphax-pos-suite-build",
  "private": true,
  "scripts": {
    "install-frontend": "cd frontend && npm install",
    "build-frontend":   "cd frontend && npm run build",
    "build":            "cd frontend && npm install && npm run build",
    "dev":              "cd frontend && npm run dev"
  }
}
